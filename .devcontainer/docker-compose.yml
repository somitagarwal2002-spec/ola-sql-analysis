version: '3'
services:
  db:
    image: postgres:15
    restart: always
    environment:
      POSTGRES_USER: user
      POSTGRES_PASSWORD: password
      POSTGRES_DB: ola_trips
    ports:
      - "5432:5432"
    volumes:
      - ./init:/docker-entrypoint-initdb.d
